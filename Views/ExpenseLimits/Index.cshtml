@model IEnumerable<Project1.Models.ExpenseLimit>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>
@Html.Raw(TempData["DeletedMessage"])

@Html.Raw(TempData["UpdatedMessage"])

@Html.Raw(TempData["InsertMessage"])

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.expense.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotExpLimit)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.expense.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotExpLimit)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ExpLimitId }) |
                @Html.ActionLink("Details", "Details", new { id = item.ExpLimitId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ExpLimitId })
            </td>
        </tr>
    }

</table>
